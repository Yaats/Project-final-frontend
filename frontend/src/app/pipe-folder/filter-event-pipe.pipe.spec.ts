import { FilterEventPipePipe } from './filter-event-pipe.pipe';

describe('FilterEventPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEventPipePipe();
    expect(pipe).toBeTruthy();
  });
});
